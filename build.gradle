plugins {
    id 'java'
    id 'application'
}

repositories {
    mavenCentral()
}

application {
    mainClass = 'game.Main'
}

run {
    standardInput = System.in
    jvmArgs = ['-Dfile.encoding=UTF-8']
}